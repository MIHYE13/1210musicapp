# Netlify 배포 가이드

## 🔒 환경 변수 보안 설정

### 중요 사항
- **API 키는 절대 프론트엔드 코드에 하드코딩하지 마세요!**
- `.env` 파일은 `.gitignore`에 포함되어 있어 Git에 커밋되지 않습니다.
- Netlify 배포 시 환경 변수는 **Netlify 대시보드**에서 설정해야 합니다.

## 📋 Netlify 환경 변수 설정 방법

### 1. Netlify 대시보드 접속
1. [Netlify 대시보드](https://app.netlify.com)에 로그인
2. 배포된 사이트 선택
3. **Site settings** > **Environment variables** 메뉴로 이동

### 2. 환경 변수 추가

#### 프론트엔드 환경 변수 (Vite)
다음 환경 변수를 추가하세요:

```
VITE_API_BASE_URL = https://your-api-server.com/api
```

**주의**: `VITE_` 접두사가 있는 환경 변수만 클라이언트 번들에 포함됩니다.

#### 백엔드 환경 변수 (선택사항)
백엔드를 Netlify Functions로 배포하는 경우에만 필요합니다:

```
OPENAI_API_KEY = sk-your-openai-api-key-here
PERPLEXITY_API_KEY = pplx-your-perplexity-api-key-here
YOUTUBE_API_KEY = AIza-your-youtube-api-key-here
```

**참고**: 현재 프로젝트는 백엔드를 별도 서버로 운영하는 구조이므로, 백엔드 환경 변수는 Netlify가 아닌 백엔드 서버에서 설정해야 합니다.

### 3. 환경 변수 범위 설정
각 환경 변수에 대해 다음 중 선택:
- **All scopes**: 모든 배포 환경에서 사용
- **Production**: 프로덕션 배포에서만 사용
- **Deploy previews**: 프리뷰 배포에서만 사용
- **Branch deploys**: 브랜치 배포에서만 사용

## ✅ 보안 확인 사항

### 1. .gitignore 확인
`.env` 파일이 `.gitignore`에 포함되어 있는지 확인:
```gitignore
.env
.env.local
```

### 2. 환경 변수 사용 패턴 확인
프론트엔드 코드에서 환경 변수 사용 시:
- ✅ 올바른 사용: `import.meta.env.VITE_API_BASE_URL`
- ❌ 잘못된 사용: `process.env.OPENAI_API_KEY` (프론트엔드에서 API 키 사용)

### 3. 빌드 확인
빌드 후 `dist` 폴더의 JavaScript 파일을 확인하여:
- API 키가 하드코딩되어 있지 않은지 확인
- `VITE_` 접두사가 없는 환경 변수는 번들에 포함되지 않아야 함

## 🔍 환경 변수 확인 방법

### 로컬 개발 환경
```bash
# .env 파일 생성
cp .env.example .env

# .env 파일 편집하여 실제 값 입력
# Windows: notepad .env
# Mac/Linux: nano .env
```

### Netlify 배포 환경
1. Netlify 대시보드에서 환경 변수 설정
2. 배포 후 브라우저 콘솔에서 확인:
```javascript
console.log(import.meta.env.VITE_API_BASE_URL)
```

## 📝 체크리스트

배포 전 확인:
- [ ] `.env` 파일이 `.gitignore`에 포함되어 있음
- [ ] `.env` 파일이 Git에 커밋되지 않았음
- [ ] Netlify 대시보드에 필요한 환경 변수 설정 완료
- [ ] API 키가 프론트엔드 코드에 하드코딩되지 않았음
- [ ] 빌드 결과물에 API 키가 포함되지 않았음

## 🚨 문제 해결

### 환경 변수가 작동하지 않는 경우
1. Netlify 대시보드에서 환경 변수가 올바르게 설정되었는지 확인
2. 환경 변수 이름이 정확한지 확인 (대소문자 구분)
3. `VITE_` 접두사가 있는지 확인 (프론트엔드 환경 변수)
4. 배포를 다시 트리거하여 환경 변수 적용 확인

### API 키 노출 우려
1. 브라우저 개발자 도구 > Sources 탭에서 번들 파일 확인
2. `grep` 또는 검색 기능으로 API 키 검색
3. 노출된 경우 즉시 키 재발급 및 Netlify 환경 변수 업데이트

## 📚 참고 자료
- [Netlify 환경 변수 문서](https://docs.netlify.com/environment-variables/overview/)
- [Vite 환경 변수 문서](https://vitejs.dev/guide/env-and-mode.html)
- [보안 모범 사례](https://owasp.org/www-project-top-ten/)
